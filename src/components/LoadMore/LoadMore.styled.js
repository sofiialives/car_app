import styled from "styled-components";

export const LoadMoreBtn = styled.button`
  font-weight: 500;
  font-size: 16px;
  color: var(--text-blue);
  text-decoration: underline;
  cursor: pointer;
`;
